name: flutter_application_1
description: "A new Flutter project."
publish_to: "none" 
version: 1.0.0+1

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter

  # UI ve İkonlar
  cupertino_icons: ^1.0.8
  
  # Bluetooth ve Navigasyon
  flutter_blue_plus: ^2.0.0
  permission_handler: ^12.0.1
  video_player: ^2.10.0
  speech_to_text: ^7.3.0
  
  # Modern UI Bileşenleri
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0
  lottie: ^3.1.0

  # Cache
  cached_network_image: ^3.3.1
  flutter_cache_manager: ^3.3.2
  
  # AR (Augmented Reality) - Basit matematik için
  vector_math: ^2.1.4
  sensors_plus: ^3.0.0
  camera: ^0.10.5+5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true